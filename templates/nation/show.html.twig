{% extends "admin/admin.html.twig" %}

{% block title %}{{ 'country.show.title' | trans }} {{ nation.name }} — {{ parent() }}{% endblock %}

{% block body %}
    <a class="btn btn-light" href="{{ path('app_nation_index') }}">{{ 'crud.back_to_list' | trans }}</a>

    <h1>{{ 'country.show.title' | trans }} {{ nation.name }}</h1>

    <table class="table table-dark table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nation.id }}</td>
            </tr>
            <tr>
                <th>{{ 'country.common.name' |trans }}</th>
                <td>{{ nation.name }}</td>
            </tr>
            <tr>
                <th>{{ 'country.common.short' | trans }}</th>
                <td>{{ nation.short }}</td>
            </tr>
            <tr>
                <th>{{ 'country.common.img' | trans }}</th>
                <td>{{ nation.img }}</td>
            </tr>
        </tbody>
    </table>

    <div class="p-1 d-flex">
        <a class="btn btn-outline-secondary" href="{{ path('app_nation_edit', {'id': nation.id}) }}">{{ 'crud.edit' | trans }}</a>

        {{ include('nation/_delete_form.html.twig') }}
    </div>

{% endblock %}
